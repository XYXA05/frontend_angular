import { AfterContentInit } from '@angular/core';
import { TerrainControl } from 'maplibre-gl';
import { MapService } from '../map/map.service';
import { ControlComponent } from './control.component';
import * as i0 from "@angular/core";
/**
 * `mglTerrain` - a terrain control directive
 *
 * @category Directives
 *
 * @see [Terrain](https://maplibre.org/ngx-maplibre-gl/demo/terrain-control)
 * @see [TerrainControl](https://maplibre.org/maplibre-gl-js/docs/API/classes/TerrainControl)
 */
export declare class TerrainControlDirective implements AfterContentInit {
    private mapService;
    private controlComponent;
    source: string;
    exaggeration?: number;
    constructor(mapService: MapService, controlComponent: ControlComponent<TerrainControl>);
    ngAfterContentInit(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<TerrainControlDirective, [null, { host: true; }]>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<TerrainControlDirective, "[mglTerrain]", never, { "source": { "alias": "source"; "required": false; }; "exaggeration": { "alias": "exaggeration"; "required": false; }; }, {}, never, never, true, never>;
}
