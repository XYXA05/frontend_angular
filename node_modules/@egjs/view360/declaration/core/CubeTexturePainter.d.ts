import Texture2D from "../texture/Texture2D";
declare class CubeTexturePainter {
    readonly texture: Texture2D;
    private _renderingOrder;
    private _canvas;
    private _ctx;
    private _row;
    private _column;
    private _size;
    get size(): number;
    constructor(texture: Texture2D, cubemapOrder: string);
    destroy(): void;
    draw(gl: WebGLRenderingContext | WebGL2RenderingContext, isWebGL2: boolean): void;
    private _calcRenderingSize;
}
export default CubeTexturePainter;
