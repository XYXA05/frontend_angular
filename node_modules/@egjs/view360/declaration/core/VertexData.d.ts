import { TypedArray } from "../type/utils";
declare class VertexData<T extends TypedArray> {
    readonly data: T;
    itemSize: number;
    count: number;
    constructor(data: T, itemSize: number);
}
export default VertexData;
