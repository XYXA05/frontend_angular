import { quat } from "gl-matrix";
import Camera from "./Camera";
type CameraPose = {
    rotation: quat;
    zoom: number;
};
declare class CameraAnimation {
    private _camera;
    private _from;
    private _to;
    private _motion;
    private _finishPromise;
    private _finish;
    get duration(): number;
    set duration(val: number);
    get easing(): (x: number) => number;
    set easing(val: (x: number) => number);
    constructor(camera: Camera, from: CameraPose, to: CameraPose, { duration, easing }?: {
        duration?: number;
        easing?: (x: number) => number;
    });
    getFinishPromise(): Promise<void>;
    update(deltaTime: number): void;
}
export default CameraAnimation;
