import Uniform from "../uniform/Uniform";
import WebGLContext from "./WebGLContext";
import { UniformLocations } from "../type/internal";
declare class ShaderProgram<T extends Record<string, Uniform> = Record<string, Uniform>> {
    readonly program: WebGLProgram;
    readonly uniforms: T;
    readonly uniformLocations: UniformLocations<T>;
    constructor(ctx: WebGLContext, vertexShader: string, fragmentShader: string, uniforms: T);
}
export default ShaderProgram;
