import Texture from "../texture/Texture";
import Texture2D from "../texture/Texture2D";
import TextureVideo from "../texture/TextureVideo";
import TextureCube from "../texture/TextureCube";
import { VideoConfig } from "../type/external";
import { ProjectionOptions } from "../projection/Projection";
declare class TextureLoader {
    private _loadChecker;
    constructor();
    load(src: ProjectionOptions["src"], video: ProjectionOptions["video"]): Promise<Texture>;
    loadImage(src: string | HTMLElement): Promise<Texture2D>;
    loadCubeImage(src: Array<string | HTMLElement>): Promise<TextureCube>;
    loadVideo(src: ProjectionOptions["src"], videoConfig: Partial<VideoConfig>): Promise<TextureVideo>;
    private _load;
    private _toImageArray;
    private _toVideoElement;
    private _appendSourceElement;
}
export default TextureLoader;
