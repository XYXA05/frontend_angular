import Component from "@egjs/component";
import { InputEvents } from "../../type/internal";
declare class TouchInput extends Component<InputEvents<{
    x: number;
    y: number;
}, TouchEvent>> {
    private _el;
    private _prevPos;
    private _isFirstTouch;
    private _scrolling;
    private _scrollable;
    get scrollable(): boolean;
    set scrollable(val: boolean);
    constructor();
    enable(element: HTMLElement): void;
    disable(): void;
    private _onTouchStart;
    private _onTouchMove;
    private _onTouchEnd;
}
export default TouchInput;
