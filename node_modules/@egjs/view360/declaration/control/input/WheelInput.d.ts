import Component from "@egjs/component";
import { InputEvents } from "../../type/internal";
declare class WheelInput extends Component<InputEvents<number, WheelEvent>> {
    private _el;
    private _scrollable;
    private _baseScale;
    private _inputTimer;
    get scrollable(): boolean;
    set scrollable(val: boolean);
    constructor();
    enable(element: HTMLElement): void;
    disable(): void;
    private _onWheel;
    private _clearTimer;
}
export default WheelInput;
