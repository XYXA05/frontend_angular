import View360Plugin from "../View360Plugin";
import View360 from "../../View360";
export interface LoadingSpinnerOptions {
    className: Partial<{
        -readonly [key in keyof typeof LoadingSpinner.DEFAULT_CLASS]: string;
    }>;
}
declare class LoadingSpinner implements View360Plugin {
    static readonly DEFAULT_CLASS: {
        readonly CONTAINER: "view360-spinner";
        readonly RING: "view360-spinner-ring";
    };
    readonly className: LoadingSpinnerOptions["className"];
    private _container;
    constructor({ className }?: Partial<LoadingSpinnerOptions>);
    init(viewer: View360): void;
    destroy(viewer: View360): void;
    private _startLoading;
    private _detachElements;
    private _createElements;
}
export default LoadingSpinner;
