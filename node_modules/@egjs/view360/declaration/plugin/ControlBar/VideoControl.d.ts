import TextureVideo from "../../texture/TextureVideo";
declare class VideoControl {
    private _video;
    enable(root: HTMLElement, video: TextureVideo): void;
    disable(root: HTMLElement): void;
    private _onKeyDown;
    private _changeVideoTime;
    private _changeVideoVolume;
    private _toggleVideo;
}
export default VideoControl;
