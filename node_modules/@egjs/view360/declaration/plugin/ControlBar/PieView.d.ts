import ControlBar from "./ControlBar";
import ControlBarItem, { ControlBarItemOptions } from "./ControlBarItem";
import View360 from "../../View360";
export interface PieViewOptions extends ControlBarItemOptions {
    resetCamera: boolean | Partial<{
        yaw: number;
        pitch: number;
        zoom: number;
        duration: number;
        easing: (x: number) => number;
    }>;
}
declare class PieView extends ControlBarItem {
    readonly element: HTMLElement;
    resetCamera: PieViewOptions["resetCamera"];
    private _viewer;
    private _piePathEl;
    private _rangeCircleEl;
    constructor({ resetCamera, position, order }?: Partial<PieViewOptions>);
    init(viewer: View360, controlBar: ControlBar): void;
    destroy(viewer: View360): void;
    private _onClick;
    private _createPieElements;
    private _updatePie;
}
export default PieView;
