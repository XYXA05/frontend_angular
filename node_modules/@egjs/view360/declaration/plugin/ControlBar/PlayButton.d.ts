import ControlBar from "./ControlBar";
import ControlBarItem, { ControlBarItemOptions } from "./ControlBarItem";
import View360 from "../../View360";
declare class PlayButton extends ControlBarItem {
    readonly element: HTMLElement;
    private _controlBar;
    private _video;
    private _paused;
    constructor({ position, order }?: Partial<ControlBarItemOptions>);
    init(viewer: View360, controlBar: ControlBar): void;
    destroy(): void;
    private _onClick;
    private _onPlay;
    private _onPause;
}
export default PlayButton;
