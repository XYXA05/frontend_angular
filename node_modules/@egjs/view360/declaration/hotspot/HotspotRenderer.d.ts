import Camera from "../core/Camera";
import WebGLRenderer from "../core/WebGLRenderer";
export interface HotspotOptions {
    zoom: boolean;
}
declare class HotspotRenderer {
    private _zoom;
    private _containerEl;
    private _renderer;
    private _hotspots;
    constructor(rootEl: HTMLElement, renderer: WebGLRenderer, { zoom }: Partial<HotspotOptions>);
    refresh(): void;
    render(camera: Camera): void;
    private _parseHotspot;
    private _yawPitchToVec3;
}
export default HotspotRenderer;
